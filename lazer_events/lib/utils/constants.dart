import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF2962FF);
  static const secondary = Color(0xFF00C853);
  static const error = Colors.red;
  static const background = Color(0xFFF5F5F5);
}

class FirestoreCollections {
  static const users = "users";
  static const events = "events";
  static const bookings = "bookings";
}

class AppText {
  static const appName = "Lazer Events";
  static const noImage =
      "https://firebasestorage.googleapis.com/v0/b/no-image.jpg"; // Cambia si quieres
}
